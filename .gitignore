.claude/
*.log
.DS_Store
Thumbs.db

# Scripts de carga masiva con datos personales sensibles (cedula, fecha de
# nacimiento, direccion, tipo de sangre, etc.) -- nunca deben publicarse en
# este repositorio, que es publico. Se corren a mano en el SQL Editor de
# Supabase y se guardan solo localmente.
sql/import_perfil_*.sql

# Igual: comparendos/accidentes de tránsito reales (cédula, nombre, lesiones)
# subidos a mano por el usuario -- nunca a git.
sql/accidentes_infracciones_import_*.sql

# Reportes/exports generados con datos personales reales de empleados
# (cedulas, nombres) para revisión local -- nunca a git.
_export_local/

# Exports de BUK u otras fuentes externas con cedulas/nombres reales que el
# usuario deja sueltos en la carpeta del proyecto para comparar -- nunca a git.
personal.csv
